#if !defined(SDLH_CONFIG_H)
#define SDLH_CONFIG_H

    /**
     * SDL Helper is not thread safe, use process instead
    */

    /**
     * @brief représente les valeurs par défaut
    */
    #define SDLH_DEFAULT -1
#endif